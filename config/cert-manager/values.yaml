installCRDs: true

cainjector:
  enabled: true

startupapicheck:
  enabled: true
  timeout: 5m

# Resource limits for cert-manager
resources:
  requests:
    cpu: "100m"
    memory: "100Mi"
  limits:
    cpu: "200m"
    memory: "300Mi"

# Security context settings
securityContext:
  enabled: true
  fsGroup: 1001
